{
  "name": "@zephyr/config",
  "description": "Zephyr configuration package contains shared configs for typescript",
  "version": "2.0.9-dev",
  "license": "AGPL-3.0",
  "private": true,
  "scripts": {
    "clean": "git clean -xdf .cache .turbo dist node_modules",
    "typecheck": "tsc --noEmit --emitDeclarationOnly false"
  }
}
